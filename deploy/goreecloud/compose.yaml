name: goreecloud-memos

services:
  memos:
    image: ${GOREECLOUD_MEMOS_IMAGE:?Set GOREECLOUD_MEMOS_IMAGE to a validated immutable image reference}
    container_name: goreecloud-memos
    restart: unless-stopped
    user: "10001:10001"
    environment:
      TZ: ${GOREECLOUD_MEMOS_TZ:-America/Chicago}
      MEMOS_PORT: "5230"
      MEMOS_DATA: /var/opt/memos
      MEMOS_DRIVER: sqlite
      MEMOS_LOG_LEVEL: info
    volumes:
      - type: bind
        source: ${GOREECLOUD_MEMOS_DATA_PATH:-/srv/docker/appdata/memos}
        target: /var/opt/memos
      - type: bind
        source: ${GOREECLOUD_MEMOS_CONFIG_PATH:-/srv/docker/secrets/memos}
        target: /etc/secrets
        read_only: true
    expose:
      - "5230"
    networks:
      - proxy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:5230/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    stop_grace_period: 20s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  proxy:
    external: true
    name: ${GOREECLOUD_PROXY_NETWORK:-proxy}
